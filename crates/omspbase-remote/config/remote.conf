# OMSPBase Remote Configuration
# Copy to /opt/oomspbase/etc/remote.conf and customize

version: 1

# Server signaling endpoint
server:
  signaling_url: "ws://localhost:9100/ws"
  # ice_servers:                       # Optional STUN/TURN URIs
  #   - "stun:stun.l.google.com:19302"

# Pre-shared key for signaling auth (default: "omspbase-dev")
# psk: "omspbase-dev"

# Render backend (optional, default: "auto")
# render:
#   backend: "auto"
